* add docstrings, including doctests

* add bindings to callback functions for integer optimization solver

* add bindings for graph functions

* add redirection of output to Python string; using tempfile?

* grab GLPK error messages and pass them to Python as some exception message